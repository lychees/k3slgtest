# RPG Maker VX Ace 素材使用规则
（依据 mkxp-z 源码整理：src/display/tilemapvx.cpp、gl/tileatlasvx.cpp、autotilesvx.cpp、windowvx.cpp 等）

## 0. 基础单位

- 一切地图素材以 **32×32 像素 = 1 格(tile)** 为基准。
- autotile 绘制时进一步细分为 **16×16 子块(subtile)**，每格 4 个象限（0=左上 1=右上 2=左下 3=右下，见 tilemap-common.h `atSelectSubPos`）。
- 一套 tileset 由 **9 张表**组成，顺序固定：`[A1, A2, A3, A4, A5, B, C, D, E]`（Tilesets.rvdata2/json 的 `tileset_names`，允许空名=缺省）。

## 1. 各表布局（像素尺寸）

| 表 | 尺寸(px) | 格数 | 内容 |
|----|---------|------|------|
| A1 | 512×384 | 16×12 | 水面/熔岩等**动画** autotile + 瀑布 |
| A2 | 512×384 | 16×12 | 地面 autotile（每块 2×3 格 = 64×96） |
| A3 | 512×256 | 16×8  | 屋顶/天花板 autotile（每块 2×2 格） |
| A4 | 512×480 | 16×15 | 墙壁：奇偶行交替 A 型顶(2×3)+B 型墙(2×2) |
| A5 | 256×512 | 8×16  | 普通单格（楼梯、地毯、杂物等） |
| B–E| 512×512 | 16×16 | 普通单格装饰（树、箱、家具…），B 只有左半 8 列起用 |

## 2. 图块 ID（tileID）编码

地图数据里存的就是最终 tileID（编辑器已把 autotile 模式烘焙进去，运行时**不做**邻接计算）：

| 范围(hex) | 表 | 解码 |
|-----------|----|------|
| 0x000–0x0FF | B | `ob=id/128; ox=id%8+(ob%2)*8; oy=(id/8)%16+(ob/2)*16`（B/C/D/E 各占 128 id，共 1024） |
| 0x100–0x3FF | C/D/E | 同上 |
| 0x600–0x67F | A5 | `ox=id%8; oy=id/8` 单格直取 |
| 0x800–0xAFF | A1 | `t=id-0x800; 模式=t%48; 块号=t/48` |
| 0xB00–0x10FF| A2 | 同上；块原点=`((块号%8)*2, (块号/8)*3)`（格） |
| 0x1100–0x16FF| A3 | 块原点=`((块号%8)*2, (块号/8)*2)`，B 型展开 |
| 0x1700–0x1FFF| A4 | `offY=[0,3,5,8,10,13]`；偶数行组=A 型，奇数=B 型 |

- 0x680–0x7FF、0x2000+ 为空/无效。
- A1 块号→图集原点：`[(0,0),(0,3),free(8,48),(8,51),(6,0),瀑布,(6,3),瀑布,(0,6),瀑布,(0,9),瀑布,(6,6),瀑布,(6,9),瀑布]`；瀑布块号 5/7/9/11/13/15 → `cID=(块号-5)/2` 取 `[(12,0),(12,3),(14,0),(14,3),(12,6),(12,9)]`。

## 3. autotile 展开（核心）

- **A 型（地面/水面）**：48 种邻接模式 × 4 子块，查 `autotileVXRectsA[模式*4+象限]` 得 16×16 源矩形（块内相对坐标），贴到格子的对应象限。
- **B 型（墙壁）**：16 种模式 × 4 子块，`autotileVXRectsB`，模式≥16 不画。
- **C 型（瀑布）**：4 种模式 × 2 条 16×32 竖条，`autotileVXRectsC`。
- **A2 桌子型**（tileset flags 0x80 标记）：6 个 quad（含伸出到下方格的"桌腿"），用 `autotileVXRectsA2` + `autotileVXRectsA2Sizes`（0 尺寸跳过）。99% 情况按 A 型处理差别不大。
- 模式索引规律：编辑器按 8 邻接（4 边 + 4 角，仅当两边都有同类时才看角）从 47 模式 blob 表中选出，烘焙进 tileID 存储。

## 4. 动画

- A1 水面：3 帧，每帧 = 块左移 2 格（`aniOffset.x = 帧*2*32`），循环序列 `0,1,2,1`，每 30 帧(0.5s@60fps) 切一次。
- 瀑布：3 帧，向下滚 1 格（`aniOffset.y = 帧*32`），循环 `0,1,2`。
- 地面/墙不动画。

## 5. 地图数据（Map00X.rvdata2 / .json）

- `data` 是 Table：`dim=3, x=宽, y=高, z=4`，扁平数组下标 = `x + y*宽 + z*宽*高`（x 最快）。
- 层序：读取顺序 z0 → z1 → z3(**阴影层**，4bit：bit0 左上、bit1 右上、bit2 左下、bit3 右下，各盖 16×16 半透明黑 #000@50%) → z2；但**盖上/盖下不按层**：任何层里带 ☆ 星标 flag(0x10) 的格画在角色上方（树梢），非星标格一律画在角色脚下（桥甲板因此在单位下方，见 mkxp tileatlasvx.cpp `OVER_PLAYER_FLAG`）。
- `tileset_id` 指向 Tilesets 表；`parallax_name` 为远景图（可循环滚动）。

## 6. 通行/功能 flags（Tilesets 的 flags 表，8192 项按 tileID 索引）

- `0x01/0x02/0x04/0x08`：下/左/右/上 四向**阻挡**位（置位=不可通行）；`0x0F` 全挡=墙/水。
- `0x10`：星标=画在角色上方（树梢）。
- `0x20`：草丛（bush，角色下半身被遮）。
- `0x40`：柜台（可隔柜对话/交易）。
- `0x80`：桌子型 A2 autotile。
- 高层位：0x100 梯子、0x200 地形伤害、0x400 计数…（SoW 里见 0x600/0xe00 组合值）。

## 7. 角色/单位 sprite（Graphics/Characters）

- 每帧 **32×32**；一个角色块 = 3 帧(走姿) × 4 方向(下/左/右/上) = 96×128。
- 文件名前缀：`$` = 表单角色（96×128 而非 384×256 八合一）；`!` = 不对齐网格擦除偏移、且不被草丛遮（物体用）。
- 朝向帧序列：行走 左-中-右-中（帧 0,1,2,1）。
- Faces 96×96/格 4×2 排布；SoW 大招/战斗特殊动作是 150×61 等单帧序列图（heroslash1-3）。

## 8. 窗口皮肤 windowskin（128×128）

- (0,0,64,64) 背景平铺区；(64,0,64,64) 边框区（16px 边框，四角 16×16，边条拉伸）；
- (0,64,32,32) 光标；(32,64,…) 四个滚动箭头/暂停符；
- (64,96,64,32) **32 色调色板**（8×4 个 8×8 格），`\C[n]` 文字色从此取色（`text_color(n)` 读像素 `64+(n%8)*8, 96+(n/8)*8`）。
- 窗口背景透明度由 `back_opacity` 控制，边框/光标独立于背景。

## 9. 战斗背景（Battlebacks1/2）

- 640×480（或更大）整幅；SoW 拆为多层：`skybox`(天) → `backdrop/background`(远) → `ground`(地，内容在下半) → `foreground`(近景遮脚)。
- 战斗单位在 3×3 石台（sowgridbase 431×151）上按阵型站位。

## 10. 已验证的参考实现

`D:\dev\k3\zq\vxace_render.py`：按上述规则把 `Data/Map00X.json` 渲染成 PNG（含 autotile 展开、阴影层、9 表图集），已用 Map003(雪原)/Map004(草原) 验证与原版一致。
